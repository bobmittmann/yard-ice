var x, y;

var x1, x2, x3, y0, y1, y2, y3;
var i;

for (i = 0; i < 64; i = i + 1) {
	x3 = x2;
	x2 = x1;
	x1 = x;
	y3 = y2;
	y2 = y1;
	y1 = y0;
	y0 = y;
	x = rand();

	y = ((x + 32768 + x1 * 100 + x2 * 233 + x3 * 9233) / 32768) - 
		((y0 + y1 * 223 + y2 * 999 + y3 * 4999) / 32767);

	print(x, y, (2 * (3 + 4 * (4 + 5))), x2, x3, y0, y1, y2, (y3 + 4));
}

