REM This is a Java Script comment

function main(argv, argc)
{
	var i;

	for (i = 0; i < 0x30; ++i) {
		printf("This is a test: %d", i);
	}
	return 0;
}

REM one line comment

let dev = sens[4];
let dev.pw5 = 4;
let dev.pw5.lookup(32);

let dev = sens[4];

sens.[5].pw3 = 0xffff;



IF number<0 THEN
  PRINT "Number is negative"
ELSEIF number>0 THEN
  PRINT "Number is positive"
ELSE
  PRINT "Number is zero"
END IF

for i = 0 to 126
for j = 0 to 126
for k = 0 to 10
let a = i * j * k
print a, i, j, k
next k
next j
next i
end

